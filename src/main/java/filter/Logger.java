package filter;
// import java.io.IOException;

// import java.io.IOException;

// import javax.servlet.FilterChain;
// import javax.servlet.FilterConfig;
// import javax.servlet.ServletException;
// import javax.servlet.ServletRequest;
// import javax.servlet.ServletResponse;
// import javax.servlet.http.HttpServletRequest;
// import javax.servlet.http.HttpServletResponse;

// import Role.Admin.ListAdmins;

// import javax.servlet.Filter;
// import javax.servlet.FilterChain;
// import javax.servlet.FilterConfig;
// import javax.servlet.ServletException;
// import javax.servlet.ServletRequest;
// import javax.servlet.ServletResponse;
// import javax.servlet.http.HttpServletRequest;
// import javax.servlet.http.HttpServletResponse;

// import Role.Admin.ListAdmins;

// @WebFilter(urlPatterns = { "/user/login" , "/user/users/home" , "/user/admin/home"})
public class Logger /* implements Filter */ {
    // @Override
    // public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain)
    //         throws IOException, ServletException {
    //     HttpServletRequest req = (HttpServletRequest) request;
    //     HttpServletResponse resp = (HttpServletResponse) response;
    //     System.out.println("Do filter");

    //     String req_username = req.getParameter("username");
    //     String req_password = req.getParameter("password");
    //     ListAdmins listAds = new ListAdmins();

    //     if (listAds != null) {
    //         boolean isAd = listAds.isAdmin(req_username, req_password);
    //         System.out.println("Filtered");
    //         if (isAd) {
    //             System.out.println("You enter admin");
    //             resp.setStatus(200);
    //         } else {
    //             resp.sendError(401);
    //         }
    //     }
    // }

    // @Override
    // public void init(FilterConfig filterConfig) throws ServletException {
    //     System.out.println("Init filter");
    // }

    // @Override
    // public void destroy() {
        
    // }
}
